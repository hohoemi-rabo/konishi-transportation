# 02: 共通レイアウト・固定ヘッダー

## 概要

App Router の `layout.tsx` にルートレイアウトを構築。固定ヘッダー（ナビゲーション + 電話番号CTA）とモバイルハンバーガーメニューを実装する。

## 要件

- 固定ヘッダー（スクロールしても上部に固定）
- ナビゲーション: 各セクションへのスムーズスクロールリンク
- 電話番号CTA: 059-328-5248（`tel:` リンク、モバイルでタップ発信）
- モバイル時: ハンバーガーメニューに切り替え
- デスクトップ時: 横並びナビゲーション
- ヘッダーは Client Component（ハンバーガー開閉の state 管理）

## 参照

- REQUIREMENTS.md §5.1 ナビゲーションヘッダー
- REQUIREMENTS.md §6 CTA（配置箇所: 固定ヘッダー）
- REQUIREMENTS.md §8 レスポンシブ対応

## Todo

- [x] `src/app/layout.tsx` にルートレイアウト構築（`<html lang="ja">`、メタデータ、フォント）
- [x] `src/components/Header.tsx` 固定ヘッダーコンポーネント作成
- [x] セクションナビゲーション（スムーズスクロール）実装
- [x] 電話番号CTAボタン配置（`tel:059-328-5248`）
- [x] モバイルハンバーガーメニュー実装
- [x] スクロール時のヘッダー背景変化（透明 → 白背景等）
- [x] レスポンシブ動作確認（モバイル / タブレット / デスクトップ）
